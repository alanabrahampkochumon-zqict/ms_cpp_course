#include <iostream> // Not including preprocessor produces errors

int main() // Double main -> Produces error
{
    std::cout << "Testing Program Structure" << std::endl;

    // Returning is optional as some compilers return a default value
}